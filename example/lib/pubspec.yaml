name: smart_barcode_reader_example
description: A simple example demonstrating the usage of smart_barcode_reader package.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  smart_barcode_reader:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true